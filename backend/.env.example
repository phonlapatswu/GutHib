# --- SharkTask Backend Configuration ---

# Database connection URL (PostgreSQL)
# Format: postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public
DATABASE_URL="postgresql://phonlapat:password@localhost:5432/sharktask?schema=public"

# Secret key for JWT signing (Should be long and random in production)
JWT_SECRET="shark_task_super_secret_key_2026"

# Port the backend server will listen on
PORT=3001

# CORS: Frontend URL
FRONTEND_URL="http://localhost:3000"
